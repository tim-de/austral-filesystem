"""
This module manages the creation and destruction of capabilities
for interacting with the file system
"""
module FileSystem is

    type FileSystemCapability: Linear;

    generic [R: Region]
    function acquireFileSystem(root: &![RootCapability, R]): FileSystemCapability;

    function releaseFileSystem(fs: FileSystemCapability): Unit;

end module.
